# Example: Copy this file to .env and fill in your values
# DO NOT commit the actual .env file to version control

JIRA_BASE_URL=https://jira.domain.com
JIRA_PAT=personal_access_token

# Optional: Custom User-Agent header for environments behind oauth2-proxy
# JIRA_USER_AGENT=your_custom_user_agent_here

# Optional: HTTP Basic Auth credentials for a reverse proxy such as Nginx Proxy Manager.
# Both values must be configured together.
# JIRA_PROXY_BASIC_AUTH_USERNAME=proxy_username
# JIRA_PROXY_BASIC_AUTH_PASSWORD=proxy_password

# Optional: Header used to carry the Jira PAT through the Basic Auth proxy.
# The default is X-Jira-Authorization.
# JIRA_PROXY_JIRA_AUTH_HEADER=X-Jira-Authorization

# Optional safety switch: keep disabled unless you explicitly need permanent issue deletion
# JIRA_ALLOW_ISSUE_DELETE=false
